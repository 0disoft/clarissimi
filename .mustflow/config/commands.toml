schema_version = "1"

[defaults]
missing_behavior = "do_not_guess"
allow_inferred_commands = false
require_lifecycle = true
require_timeout_for_oneshot = true
deny_unmanaged_long_running = true
default_cwd = "."
default_timeout_seconds = 600
stdin = "closed"
max_output_bytes = 1048576
on_timeout = "terminate_process_tree"
kill_after_seconds = 5
env_policy = "minimal"
env_allowlist = []

[resources.build_outputs]
type = "path"
paths = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
concurrency = "exclusive_writer"
description = "Ignored TypeScript workspace build outputs and incremental metadata."

[resources.action_bundle]
type = "path"
paths = ["action-dist/index.js"]
concurrency = "exclusive_writer"
description = "Tracked GitHub Action runtime bundle generated from the maintained Action source."

[resources.standalone_cli_package]
type = "path"
paths = [".tmp/npm/clarissimi/**"]
concurrency = "exclusive_writer"
description = "Ignored standalone CLI npm staging output rebuilt from maintained source files."

[resources.git_state]
type = "path"
paths = [".git/index", ".git/refs/**", ".git/logs/**", ".git/objects/**"]
concurrency = "exclusive_writer"
description = "Clarissimi local Git index, commit objects, refs, and reflogs."

[resources.mustflow_contract]
type = "path"
paths = [".mustflow/config/commands.toml", ".mustflow/config/manifest.lock.toml"]
concurrency = "exclusive_writer"
description = "Clarissimi command authority and its mustflow content-hash lock."

[resources.docs_review_queue]
type = "path"
paths = [".mustflow/review/docs.toml"]
concurrency = "exclusive_writer"
description = "Repository-local documentation review queue and audit record."

[intents.changes_status]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Show concise Clarissimi worktree state without modifying Git state."
argv = ["git", "status", "--short", "--branch"]
cwd = "."
timeout_seconds = 30
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["before_edit", "before_final_report"]

[intents.changes_diff_summary]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Summarize unstaged Clarissimi changes without modifying Git state."
argv = ["git", "diff", "--stat"]
cwd = "."
timeout_seconds = 30
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["code_change", "documentation_change", "before_final_report"]

[intents.build]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Compile all Clarissimi TypeScript workspace packages."
argv = ["pnpm", "run", "build"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["typescript_change", "public_api_change"]

[intents.test_related]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build the workspace and run the complete Clarissimi test suite used for related feature changes."
argv = ["pnpm", "run", "test"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["behavior_change", "test_change", "schema_change"]

[intents.test_release_readiness]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run the focused release-readiness contract regression suite without rebuilding unrelated packages."
argv = ["node", "--test", "scripts/test/release-readiness.test.mjs"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["release_policy_change", "release_readiness_contract_change"]

[intents.benchmark_scale]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and run the deterministic 1,000- and 10,000-record scale regression guard."
argv = ["pnpm", "run", "benchmark:scale"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["renderer_hot_path_change", "redaction_hot_path_change", "scale_benchmark_change"]

[intents.benchmark_scale_sample]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and emit three environment-specific scale benchmark samples for local investigation."
argv = ["pnpm", "run", "benchmark:scale:sample"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["performance_investigation_requested"]

[intents.benchmark_cli_io]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and run deterministic real-CLI rebuild and atomic import file-I/O regression guards."
argv = ["pnpm", "run", "benchmark:cli-io"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["cli_rebuild_io_change", "cli_import_atomic_io_change", "cli_io_benchmark_change"]

[intents.benchmark_cli_io_sample]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and emit three environment-specific real CLI file-I/O benchmark samples."
argv = ["pnpm", "run", "benchmark:cli-io:sample"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["cli_io_performance_investigation_requested"]

[intents.test_renderer_output_optimization]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build renderers and run focused output parity, validation, and scale benchmark contract regressions."
argv = [
  "node",
  "--test",
  "packages/renderers/test/renderers.test.mjs",
  "scripts/test/benchmark-scale.test.mjs",
]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/renderers/dist/index.js", sources = [
    "packages/core/src/**",
    "packages/schemas/src/**",
    "packages/renderers/src/**",
    "packages/renderers/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
network = false
destructive = false
required_after = ["renderer_output_optimization_change", "renderer_output_optimization_test_change"]

[intents.test_cli_io_benchmark]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused real subprocess, filesystem integrity, residue, failure, and argument regressions for the CLI I/O benchmark."
argv = ["node", "--test", "scripts/test/benchmark-cli-io.test.mjs"]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/cli/dist/bin/clarissimi.js", sources = [
    "packages/cli/src/**",
    "packages/core/src/**",
    "packages/renderers/src/**",
    "packages/schemas/src/**",
    "packages/cli/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
network = false
destructive = false
required_after = ["cli_io_benchmark_change", "cli_io_benchmark_test_change"]

[intents.test_automation_contributors]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build and run schema, GitHub, renderer, CLI, and Action tests covering automation contributor behavior."
argv = ["pnpm", "run", "test:automation-contributors"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["automation_contributor_change", "automation_contributor_test_change"]

[intents.test_cli_lock_concurrency]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run bounded Windows file-operation retry checks and the concurrent ledger import stress regression with worker failure diagnostics."
argv = ["pnpm", "run", "test:cli-lock"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["cli_lock_change", "cli_lock_test_change"]

[intents.test_cli_shell_completion]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build and run deterministic Bash, Zsh, fish, and PowerShell completion contract regressions."
argv = ["pnpm", "run", "test:cli-completion"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["cli_completion_change", "cli_completion_test_change"]

[intents.lint]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run Oxlint with warnings denied over maintained source files."
argv = ["pnpm", "run", "lint"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["typescript_change", "javascript_change"]

[intents.format]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Check maintained files with Oxfmt without rewriting them."
argv = ["pnpm", "run", "format"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["source_change", "documentation_change", "structured_config_change"]

[intents.format_write]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Rewrite maintained Clarissimi source, configuration, and documentation with Oxfmt."
argv = ["pnpm", "exec", "oxfmt", "--write"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = [
  "**/*.ts",
  "**/*.js",
  "**/*.mjs",
  "**/*.json",
  "**/*.md",
  "**/*.toml",
  "**/*.yml",
  "**/*.yaml",
]
effects = [
  { type = "write", mode = "replace", path = "**/*.ts", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "**/*.mjs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "**/*.json", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "**/*.md", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "**/*.toml", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "**/*.yml", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["before_format_check_when_maintained_files_changed"]

[intents.docs_validate_fast]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Validate required Clarissimi documentation, links, examples, and workflow documents."
argv = ["pnpm", "run", "docs"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["documentation_change", "public_contract_change"]

[intents.docs_review_add_changed]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Queue changed Clarissimi documentation for explicit review without changing product files."
argv = ["mf", "docs", "review", "add", "--changed"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = [".mustflow/review/docs.toml"]
effects = [
  { type = "write", mode = "replace", path = ".mustflow/review/docs.toml", lock = "docs_review_queue", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["documentation_change"]

[intents.release_readiness]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run non-credentialed Clarissimi release contract and drift checks."
argv = ["pnpm", "run", "release-readiness"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["release_contract_change", "public_contract_change", "before_final_report"]

[intents.migration_check]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build schemas and verify persisted assessment compatibility and deterministic migrations."
argv = ["pnpm", "run", "migration-check"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["schema_change", "migration_change"]

[intents.smoke]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build and run local CLI, Action, package, and workflow smoke checks."
argv = ["pnpm", "run", "smoke"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["cli_change", "action_change", "packaging_change"]

[intents.check]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run the Clarissimi typecheck and complete package test suite."
argv = ["pnpm", "run", "check"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["code_change", "test_change", "before_final_report"]

[intents.contract]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run the Clarissimi typecheck and test contract gate."
argv = ["pnpm", "run", "contract"]
cwd = "."
timeout_seconds = 1200
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["public_contract_change", "schema_change", "before_final_report"]

[intents.bundle_action]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and regenerate the tracked GitHub Action runtime bundle."
argv = ["pnpm", "run", "bundle:action"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo", "action-dist/index.js"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "action-dist/index.js", lock = "action_bundle", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["action_source_change", "action_dependency_change"]

[intents.bundle_action_check]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build Clarissimi and verify the tracked Action bundle is fresh without rewriting it."
argv = ["pnpm", "run", "bundle:action:check"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["action_bundle_generation", "before_final_report"]

[intents.test_release]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run the local non-credentialed release-readiness gate."
argv = ["pnpm", "run", "release-readiness"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["release_sensitive_change", "before_final_report"]

[intents.mustflow_check]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Validate the Clarissimi mustflow command contract and repository workflow installation."
argv = ["mf", "doctor", "--json"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["mustflow_config_change", "before_final_report"]

[intents.test_historical_command_intent_retirement]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused regressions for completed release and milestone command-intent retirement."
argv = ["node", "--test", "scripts/test/retire-historical-command-intents.test.mjs"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["historical_command_intent_policy_change"]

[intents.retire_historical_command_intents]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Remove explicitly completed release and milestone command buttons, preserve shared tools, and make historical exact-tag revalidation manual-only."
argv = ["pnpm", "run", "retire-historical-command-intents"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = [".mustflow/config/commands.toml", ".mustflow/config/manifest.lock.toml"]
effects = [
  { type = "write", mode = "replace", path = ".mustflow/config/commands.toml", lock = "mustflow_contract", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = ".mustflow/config/manifest.lock.toml", lock = "mustflow_contract", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["completed_release_record_verified", "completed_milestone_list_reviewed"]

[intents.git_stage_historical_command_retirement]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
approval_actions = ["git_commit"]
description = "Stage only the reviewed historical command-retirement implementation, contract, tests, and manifest lock."
argv = [
  "git",
  "add",
  "--",
  ".mustflow/config/commands.toml",
  ".mustflow/config/manifest.lock.toml",
  "scripts/retire-historical-command-intents.mjs",
  "scripts/test/retire-historical-command-intents.test.mjs",
]
cwd = "."
timeout_seconds = 60
stdin = "closed"
success_exit_codes = [0]
writes = [".git/index"]
effects = [
  { type = "write", mode = "replace", path = ".git/index", lock = "git_state", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["historical_command_intent_retirement_validation_complete"]

[intents.git_commit_historical_command_retirement]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
approval_actions = ["git_commit"]
description = "Commit the reviewed retirement of completed version-fixed release command intents."
argv = [
  "git",
  "commit",
  "-m",
  "chore(mustflow): retire completed release intents",
  "-m",
  "Validation: historical-command-intent-retirement, release-readiness, format, mustflow",
]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = [".git/index", ".git/refs/**", ".git/logs/**", ".git/objects/**"]
effects = [
  { type = "write", mode = "replace", path = ".git/**", lock = "git_state", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["git_stage_historical_command_retirement"]

[intents.git_stage_workspace_package_policy_diagnostics]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
approval_actions = ["git_commit"]
description = "Stage only the reviewed workspace-package release-policy diagnostic wording, its regression test, command contract, and manifest lock."
argv = [
  "git",
  "add",
  "--",
  ".mustflow/config/commands.toml",
  ".mustflow/config/manifest.lock.toml",
  "scripts/release-readiness.mjs",
  "scripts/test/release-readiness.test.mjs",
]
cwd = "."
timeout_seconds = 60
stdin = "closed"
success_exit_codes = [0]
writes = [".git/index"]
effects = [
  { type = "write", mode = "replace", path = ".git/index", lock = "git_state", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["workspace_package_policy_diagnostics_validation_complete"]

[intents.git_commit_workspace_package_policy_diagnostics]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
approval_actions = ["git_commit"]
description = "Commit the reviewed release-readiness diagnostic distinction between private workspace packages and the standalone CLI distribution."
argv = [
  "git",
  "commit",
  "-m",
  "fix(release): scope package policy diagnostics",
  "-m",
  "Validation: release-readiness, format, mustflow",
]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = [".git/index", ".git/refs/**", ".git/logs/**", ".git/objects/**"]
effects = [
  { type = "write", mode = "replace", path = ".git/**", lock = "git_state", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["git_stage_workspace_package_policy_diagnostics"]

[intents.bundle_standalone_cli_package]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build the workspace and stage the dependency-free standalone CLI package under the ignored bounded output directory."
argv = ["pnpm", "run", "bundle:cli-package"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = [
  "packages/*/dist/**",
  "*.tsbuildinfo",
  "packages/*/*.tsbuildinfo",
  ".tmp/npm/clarissimi/**",
]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "*.tsbuildinfo", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "packages/*/*.tsbuildinfo", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = ".tmp/npm/clarissimi/**", lock = "standalone_cli_package", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = ["standalone_cli_distribution_change"]

[intents.verify_standalone_cli_package]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build, pack, install, and execute the standalone CLI in an isolated temporary consumer without registry publication."
argv = ["pnpm", "run", "verify:cli-package"]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = [
  "packages/*/dist/**",
  "*.tsbuildinfo",
  "packages/*/*.tsbuildinfo",
  ".tmp/npm/clarissimi/**",
]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "*.tsbuildinfo", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = "packages/*/*.tsbuildinfo", lock = "build_outputs", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = ".tmp/npm/clarissimi/**", lock = "standalone_cli_package", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = [
  "standalone_cli_distribution_change",
  "npm_publish_workflow_change",
  "before_npm_publication",
]

[intents.test_standalone_cli_package]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused standalone CLI manifest, tarball-surface, workflow, and release-readiness contract regressions."
argv = [
  "node",
  "--test",
  "scripts/test/build-standalone-cli-package.test.mjs",
  "scripts/test/release-readiness.test.mjs",
]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["standalone_cli_distribution_change", "standalone_cli_distribution_test_change"]

[intents.stage_standalone_cli_package]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "npm staging mutates an external registry, requires configured stage-only OIDC, and reserves a package version pending interactive maintainer review."
agent_action = "do_not_run_report_manual_only"
description = "Submit the verified standalone CLI to npm staging after every registry, provenance, and exact-SHA gate passes."
argv = ["npm", "stage", "publish", "--access", "public", "--provenance"]
cwd = ".tmp/npm/clarissimi"
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "minimal"
env_allowlist = []
network = true
destructive = false

[intents.release_v0_5_2_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
approval_actions = ["release"]
description = "Revalidate published v0.5.2 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.5.2",
  "--external-ref",
  "v0.5.2",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_5_1_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
approval_actions = ["release"]
description = "Revalidate published v0.5.1 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.5.1",
  "--external-ref",
  "v0.5.1",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_5_0_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
approval_actions = ["release"]
description = "Revalidate published v0.5.0 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.5.0",
  "--external-ref",
  "v0.5.0",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.git_push_main]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
approval_actions = ["git_push"]
description = "Push the validated Clarissimi main branch to origin without force."
argv = ["git", "push", "origin", "main"]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = true
destructive = false
required_after = ["validated_commit_on_main", "user_authorized_push"]

[intents.release_v0_4_0_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.4.0 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.4.0",
  "--external-ref",
  "v0.4.0",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.hosted_ci_main]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Verify the hosted CI result for the current pushed main commit without reading repository secrets."
argv = ["pnpm", "run", "hosted-ci-validation"]
cwd = "."
timeout_seconds = 1800
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false
required_after = ["git_push_main"]

[intents.test_release_publication]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused Clarissimi release evidence and immutable publication regression tests."
argv = [
  "node",
  "--test",
  "scripts/test/action-release-version.test.mjs",
  "scripts/test/hosted-external-consumer-smoke.test.mjs",
  "scripts/test/release-candidate-evidence-issue.test.mjs",
  "scripts/test/release-candidate-evidence-orchestrator.test.mjs",
  "scripts/test/publish-action-release.test.mjs",
  "scripts/test/promote-action-major-alias.test.mjs",
  "scripts/test/verify-action-major-tag.test.mjs",
  "scripts/test/verify-marketplace-release.test.mjs",
]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["release_evidence_change", "release_publication_change"]

[intents.test_major_alias_promotion]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused CAS promotion, alias verification, and hosted evidence contract regressions."
argv = [
  "node",
  "--test",
  "scripts/test/action-release-version.test.mjs",
  "scripts/test/hosted-external-consumer-smoke.test.mjs",
  "scripts/test/promote-action-major-alias.test.mjs",
  "scripts/test/verify-action-major-tag.test.mjs",
  "scripts/test/release-candidate-evidence-orchestrator.test.mjs",
  "scripts/test/release-candidate-evidence-issue.test.mjs",
]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["major_alias_promotion_change", "major_alias_evidence_change"]

[intents.release_v0_2_0_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate the published v0.2.0 tag with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "versioned-action-tag",
  "--release-version",
  "v0.2.0",
  "--external-ref",
  "v0.2.0",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.test_marketplace_release]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run focused Marketplace release metadata, evidence, publisher, and release-policy regressions."
argv = [
  "node",
  "--test",
  "scripts/test/action-release-version.test.mjs",
  "scripts/test/release-candidate-evidence-issue.test.mjs",
  "scripts/test/release-candidate-evidence-orchestrator.test.mjs",
  "scripts/test/publish-action-release.test.mjs",
  "scripts/test/release-readiness.test.mjs",
  "scripts/test/verify-marketplace-release.test.mjs",
]
cwd = "."
timeout_seconds = 600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["marketplace_release_change"]

[intents.release_v0_3_0_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.0 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.0",
  "--external-ref",
  "v0.3.0",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_3_1_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.1 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.1",
  "--external-ref",
  "v0.3.1",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_3_2_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.2 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.2",
  "--external-ref",
  "v0.3.2",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_3_3_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.3 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.3",
  "--external-ref",
  "v0.3.3",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.test_provider_result_quality]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Run the focused deterministic provider result quality corpus and provider adapter regressions."
argv = [
  "node",
  "--test",
  "packages/providers/test/result-quality-corpus.test.mjs",
  "packages/providers/test/fake-provider.test.mjs",
  "packages/providers/test/openai-compatible-provider.test.mjs",
]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/providers/dist/index.js", sources = [
    "packages/providers/src/**",
    "packages/providers/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
required_after = [
  "provider_result_quality_change",
  "provider_prompt_change",
  "provider_quality_corpus_change",
]

[intents.release_v0_3_4_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.4 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.4",
  "--external-ref",
  "v0.3.4",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.release_v0_3_5_post_tag_evidence]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
reason = "Historic exact-tag revalidation dispatches credentialed external and full-write workflows."
agent_action = "do_not_run_report_manual_only"
description = "Revalidate published v0.3.5 with hosted live-provider, external dry-run, full-write, and orphan-audit workflows."
argv = [
  "pnpm",
  "run",
  "release-candidate-evidence-orchestrator",
  "--",
  "--provider-model",
  "gpt-4.1-mini",
  "--release-type",
  "marketplace-action-tag",
  "--release-version",
  "v0.3.5",
  "--external-ref",
  "v0.3.5",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
env_policy = "allowlist"
env_allowlist = ["APPDATA", "USERPROFILE"]
network = true
destructive = false

[intents.mustflow_update_dry_run]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Preview the installed Mustflow workflow update plan with bounded diffs and machine-readable blocking state."
argv = ["mf", "update", "--dry-run", "--diff", "--json"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["mustflow_install_state_review"]

[intents.mustflow_update_apply]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Apply a previously clean Mustflow workflow update plan, including managed templates, backups, and manifest-lock state."
argv = ["mf", "update", "--apply"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = ["AGENTS.md", ".mustflow/**"]
effects = [
  { type = "write", mode = "replace", path = "AGENTS.md", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = ".mustflow/**", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = [
  "mustflow_update_dry_run",
  "mustflow_update_plan_has_no_blockers",
  "user_authorized_workflow_repair",
]

[intents.mustflow_init_recovery_dry_run]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Preview a non-interactive Mustflow installation-state recovery when the manifest lock is missing."
argv = ["mf", "init", "--dry-run", "--yes"]
cwd = "."
timeout_seconds = 120
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
required_after = ["mustflow_update_failed_only_for_missing_manifest_lock"]

[intents.mustflow_init_recovery_apply]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Apply a reviewed non-interactive Mustflow installation-state recovery without force or interactive prompts."
argv = ["mf", "init", "--yes"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = ["AGENTS.md", ".mustflow/**"]
effects = [
  { type = "write", mode = "replace", path = "AGENTS.md", concurrency = "exclusive" },
  { type = "write", mode = "replace", path = ".mustflow/**", concurrency = "exclusive" },
]
network = false
destructive = false
required_after = [
  "mustflow_init_recovery_dry_run",
  "mustflow_init_plan_has_no_conflicts",
  "user_authorized_workflow_repair",
]

[intents.test_action_provider_failure_summary]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build and run the focused Action regression for bounded provider-result quality failure summaries."
argv = [
  "node",
  "--test",
  "--test-name-pattern=environment runner writes structured provider quality failures",
  "packages/action/test/action.test.mjs",
]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/action/dist/index.js", sources = [
    "packages/action/src/**",
    "packages/providers/src/**",
    "packages/schemas/src/**",
    "packages/action/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
required_after = ["action_provider_failure_summary_change"]

[intents.test_action_propose_runner]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build the Action package and run isolated proposal branch, local remote, pull request, and direct commit runner regressions."
argv = ["node", "--test", "packages/action/test/propose-runner.test.mjs"]
cwd = "."
timeout_seconds = 300
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/action/dist/index.js", sources = [
    "packages/action/src/**",
    "packages/core/src/**",
    "packages/github/src/**",
    "packages/providers/src/**",
    "packages/redaction/src/**",
    "packages/renderers/src/**",
    "packages/schemas/src/**",
    "packages/action/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
network = false
destructive = false
required_after = ["action_propose_runner_failure", "action_propose_runner_change"]

[intents.test_provider_result_dirty_corpus]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build providers and run the focused scrubbed dirty-corpus contract, privacy, and promoted-case regressions."
argv = ["node", "--test", "packages/providers/test/result-quality-dirty-corpus.test.mjs"]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/providers/dist/index.js", sources = [
    "packages/providers/src/**",
    "packages/schemas/src/**",
    "packages/providers/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
required_after = ["provider_result_dirty_corpus_change"]

[intents.provider_model_eval_check]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Validate the versioned multi-model provider eval matrix example and accepted golden-case selection without credentials or network calls."
argv = [
  "node",
  "scripts/provider-model-eval.mjs",
  "--check",
  "--matrix",
  "fixtures/provider-model-eval.example.json",
]
cwd = "."
timeout_seconds = 60
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/providers/dist/index.js", sources = [
    "packages/core/src/**",
    "packages/providers/src/**",
    "packages/schemas/src/**",
    "packages/core/tsconfig.json",
    "packages/providers/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
required_after = ["provider_model_eval_matrix_change", "provider_model_eval_script_change"]

[intents.provider_model_eval_live]
status = "manual_only"
lifecycle = "oneshot"
run_policy = "manual_only"
description = "Run the credentialed, networked, cost-bearing provider model matrix sequentially from the ignored maintainer-local config."
argv = [
  "pnpm",
  "run",
  "provider-model-eval",
  "--",
  "--matrix",
  ".clarissimi/provider-model-eval.local.json",
]
cwd = "."
timeout_seconds = 3600
stdin = "closed"
success_exit_codes = [0]
writes = ["packages/*/dist/**", "*.tsbuildinfo", "packages/*/*.tsbuildinfo"]
effects = [
  { type = "write", mode = "replace", path = "packages/*/dist/**", lock = "build_outputs", concurrency = "exclusive" },
]
env_policy = "inherit"
network = true
destructive = false
reason = "Live model evaluation consumes maintainer credentials, external network, time, and provider budget."
agent_action = "do_not_run_report_manual_only"

[intents.test_provider_model_eval]
status = "configured"
lifecycle = "oneshot"
run_policy = "agent_allowed"
description = "Build and run the credential-free provider model eval check, preflight, and sanitized-report regressions."
argv = ["node", "--test", "scripts/test/provider-model-eval.test.mjs"]
cwd = "."
timeout_seconds = 180
stdin = "closed"
success_exit_codes = [0]
writes = []
effects = []
network = false
destructive = false
preconditions = [
  { kind = "artifact_freshness", artifact = "packages/providers/dist/index.js", sources = [
    "packages/core/src/**",
    "packages/providers/src/**",
    "packages/schemas/src/**",
    "packages/core/tsconfig.json",
    "packages/providers/tsconfig.json",
    "tsconfig.json",
  ], satisfy_intent = "build" },
]
required_after = ["provider_model_eval_script_change", "provider_model_eval_test_change"]
